<p>タスク一覧</p>

<ul>
  <% @task.each do |task| %>
    <li><%= link_to task.id, task_path(task) %> : <%= task.status %> : <%= task.content %></li>
  <% end %>
</ul>

<%= link_to "新規タスクを登録する", new_task_path %>